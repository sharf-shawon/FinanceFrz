services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    # image: ghcr.io/sharf-shawon/financefrz:latest
    pull_policy: always
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL_PROD:-file:/app/data/prod.db}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - NEXTAUTH_URL=${NEXTAUTH_URL}
      - RESEND_API_KEY=${RESEND_API_KEY}
      - EMAIL_FROM=${EMAIL_FROM:-noreply@financefrz.com}
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
